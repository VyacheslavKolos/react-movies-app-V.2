import React, {FC} from 'react';
import {MoviesList} from "../MoviesList/MoviesList";

const Main: FC = () => {

    return (
        <div>
            <MoviesList/>
        </div>
    );
};

export {Main};